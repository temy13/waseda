
import java.util.ArrayList;

public class RoomControl {
/*
	static ArrayList<Room> rooms = new ArrayList<Room>();
	
	//各ホテルで、その日空いている部屋があるかのチェック
	public static boolean HaveEmpty(int hotel_id,int date){
		
		for(Room element : rooms) {
			if(hotel_id == element.hotel_id){//その部屋が今見ているホテルの部屋で、
				if(element.ReservationDate.indexOf(date) == -1)//かつその日付で空いてる部屋が一つでもあればOK
						return true;
			}
		}
		return false;
	}
	
	public static ArrayList<Room> SearchResult(Hotel hotel,int date){
		ArrayList<Room> SearchedRooms = new ArrayList<Room>();
		
		for(Room element : rooms) {
			//まずホテルが違ったらその時点で違う
			if(element.hotel_id != hotel.id)
				continue;
			//今見ている部屋に空きがあるかチェック
			if(element.Empty(date));//あったら
					SearchedRooms.add(element);//追加
		
		}
		
		return SearchedRooms;
	}
	*/
}
