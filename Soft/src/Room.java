import java.util.ArrayList;

public class Room{
/*	int hotel_id;
	int room_number;
	int max;
	int cost;
	boolean use;
	public ArrayList<Integer> ReservationDate = new ArrayList<Integer>();//その部屋の予約が入っている日付のリスト
	
	public Room(int hotel_id,int room_number, int max,int cost){
		this.hotel_id =hotel_id;
		this.room_number=room_number;
		this.max = max;
		this.cost = cost;
		this.use = false;
	}
	
	//指定された日付でこの部屋が空かどうか
	public boolean Empty(int date){
		
		if(ReservationDate.isEmpty())
			return true;
		if(ReservationDate.indexOf(date) != -1)
				return false;
		else
			return true;
	}*/
	
}